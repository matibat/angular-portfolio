import { DisplayrDirective } from './displayr.directive';

describe('DisplayrDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayrDirective();
    expect(directive).toBeTruthy();
  });
});
